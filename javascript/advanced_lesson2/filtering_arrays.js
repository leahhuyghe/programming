// filter is a function, right?
var filter = function (x) {

// now define the other function, called fn.
var fn = function (fn){
  // when x is passed into the function, it should be greater than 0 to be +.
  for (x >= 0 {
    // if it meets the above, return true.
    return 'true' } else {
    return 'false'
  })
  };
};

// tried to compound a section from possibly_positive but it just
// screwed it up even further.
